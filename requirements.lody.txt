# Dépendances de l'interface Lody Video Factory (image Dockerfile.lody).
# Volontairement minimales : le moteur de génération (MoviePy, fournisseurs…)
# sera ajouté quand il sera branché — voir requirements.txt pour la liste complète.
streamlit==1.59.1
# Déjà installée transitivement par streamlit (dépendance obligatoire de son rendu d'image) : épinglée ici
# explicitement car lody.reference_images (#38) en dépend directement pour valider le contenu réel d'une
# image de référence (jamais son extension ni son Content-Type déclaré) — aucune dépendance nouvelle ajoutée.
pillow>=11,<13
